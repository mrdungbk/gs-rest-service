<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE sqlMap PUBLIC "-//ibatis.apache.org//DTD SQL Map 2.0//EN" "http://ibatis.apache.org/dtd/sql-map-2.dtd">

<sqlMap namespace="Employee">

    <!-- Perform Insert Operation -->
    <insert id="insert" parameterClass="database.entity.Employee">
        INSERT INTO EMPLOYEE(first_name, last_name, salary)
        values (#first_name#, #last_name#, #salary#)

        <selectKey resultClass="int" keyProperty="id">
            select last_insert_id() as id
        </selectKey>
    </insert>

    <!-- Perform Read Operation -->
    <select id="getAll" resultClass="database.entity.Employee">
        SELECT * FROM EMPLOYEE
    </select>

    <!-- Perform Update Operation -->
    <update id="update" parameterClass="database.entity.Employee">
        UPDATE EMPLOYEE
        SET    first_name = #first_name#
        WHERE  id = #id#
    </update>

    <!-- Perform Delete Operation -->
    <delete id="delete" parameterClass="int">
        DELETE FROM EMPLOYEE
        WHERE  id = #id#
    </delete>

    <parameterMap id="getEmpInfoCall" class="map">
        <parameter property="acctID" jdbcType="INT" javaType="java.lang.Integer" mode="IN"/>
    </parameterMap>

    <!-- To call stored procedure. -->
    <procedure id="getEmpInfo" resultClass="database.entity.Employee" parameterMap="getEmpInfoCall">
        { call getEmp( #acctID# ) }
    </procedure>
</sqlMap>